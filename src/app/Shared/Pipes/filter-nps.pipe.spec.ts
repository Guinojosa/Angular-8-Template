import { FilterNpsPipe } from './filter-nps.pipe';

describe('FilterNpsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNpsPipe();
    expect(pipe).toBeTruthy();
  });
});
